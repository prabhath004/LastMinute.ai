# lib/

Shared utility functions and helpers.

| File       | Contents                                      |
| ---------- | --------------------------------------------- |
| `utils.ts` | `cn()` — Tailwind class merging (clsx + twMerge) |

Add new utilities here when they're used across multiple parts of the app (agents, components, API routes).
