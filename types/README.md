# types/

Shared TypeScript type definitions used across the app.

| File        | Contents                                                  |
| ----------- | --------------------------------------------------------- |
| `index.ts`  | Core domain types: concepts, courses, scenarios, quizzes  |

Import anywhere via `@/types`.
